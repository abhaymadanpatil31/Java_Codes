
class ArrayDemo {
	
	public static void main(String [] args) {
	
		int [] arr=new int [5];//if we not mention the Dimension then it will show error but this kind of initialization is allowed in cpp and if we give dimension then it will not show error but cpp will.
		//System.out.println(arr.capacity());this method is not available in array.
		System.out.println(arr.length);
	//	System.out.println(arr.indexOf());//not available
	}
}
